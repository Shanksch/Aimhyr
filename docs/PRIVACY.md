 # Privacy summary (MVP)

Default behavior:
- Recordings are processed for transcription in-session and **deleted immediately** after transcription unless the user explicitly opts-in to save.
- Transcripts and scores are stored for 30 days by default and deleted automatically.
- No third-party sharing; if an external API (LLM/STT) is used, the user sees a "Using external service: X" notice.
